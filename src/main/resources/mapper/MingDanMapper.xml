<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cupk.mapper.MingDanMapper">
    <select id="findAll" resultType="com.cupk.pojo.MingDan">
        SELECT * FROM mingdan LIMIT #{size} OFFSET #{page}
    </select>
    <select id="findMingDanByStr" parameterType="java.lang.String" resultType="com.cupk.pojo.MingDan">
        SELECT * FROM mingdan
        WHERE 1=1
        <if test="searchStr!=null and searchStr!=''">
            and id like CONCAT('%',#{searchParam},'%')
            and mingcheng like CONCAT('%',#{searchParam},'%')
            and caixi like CONCAT('%',#{searchParam},'%')
            and diqu like CONCAT('%',#{searchParam},'%')
            and jiage like CONCAT('%',#{searchParam},'%')
            and zhuyaopeiliao like CONCAT('%',#{searchParam},'%')
        </if>

    </select>

</mapper>